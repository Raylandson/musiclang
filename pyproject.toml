[project]
name = "musiclang"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raylandson", email = "www.raylandsoncs@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mido>=1.3.3",
    "pytest>=9.1.1",
]

[project.scripts]
musiclang = "musiclang:main"

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"
